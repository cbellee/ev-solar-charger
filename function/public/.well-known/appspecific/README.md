The Tesla public key is served dynamically by the Azure Function from the `TESLA_PUBLIC_KEY_PEM` app setting.

Do not commit the live public key here unless you intentionally switch to static file hosting.
